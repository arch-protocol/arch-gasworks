name: Gasworks Permit2 Tests

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  permit2-swap-tests:
    uses: ./.github/workflows/tests-template.yml
    with:
      make-command: test-permit2-swap
    secrets: inherit