name: Gasworks Permit2 Tests

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  gasworks-tests:
    uses: ./.github/workflows/tests-template.yml
    with:
      make-command: test-permit2
    secrets: inherit